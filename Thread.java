What is the difference between wait(), sleep(), and join()?
Answer:

wait(): Releases lock and waits until notify() or notifyAll() is called.

sleep(): Pauses thread for a given time without releasing lock.

join(): Waits for another thread to finish before continuing.
